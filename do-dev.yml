alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED
domains:
  - domain: api-dev.icebreakers.blockfrost.io
    type: PRIMARY
features:
  - buildpack-stack=ubuntu-22
ingress:
  rules:
    - component:
        name: blockfrost-icebreakers-api
      match:
        path:
          prefix: /
name: blockfrost-icebreakers-api-dev
region: fra
services:
  - dockerfile_path: Dockerfile
    envs:
      - key: DATABASE_URL
        scope: RUN_TIME
        value: ${db.DATABASE_URL}
    github:
      branch: master
      deploy_on_push: true
      repo: blockfrost/blockfrost-icebreakers-api
    http_port: 3000
    instance_count: 2
    instance_size_slug: apps-s-1vcpu-1gb
    name: blockfrost-icebreakers-api
    source_dir: /
